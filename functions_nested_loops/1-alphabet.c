#include "main.h"

/**
 * main - Entry point working with alphabet and main.h
 * Return: Always 0 (Success)
 */

int main(void)
{
	print_alphabet();  /* Call the function to print the alphabet */
	return (0);
}
