#include "main.h"

/**
 * main - Entry point working with alphabet x10 and main.h
 * *Return: Always 0 (Success)
 */
int main(void)
{
	print_alphabet_x10();/* Call the function to print the alphabet */
	return (0);
}
