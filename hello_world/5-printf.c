#include <stdio.h>
/**
 * learning to use prinf instead of puts
 * main - Entry point
 * Return: Always 0
 */
int main(void)
{
	printf("\"with proper grammar, but the outcome is a piece of art,\n");
}
